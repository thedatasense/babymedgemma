# Heavy artifacts live on Hugging Face (saillab/babymedgemma), not in git
# MedSigLIP feature cache (~1 GB)
cache/
# per-run checkpoints (~4 GB) in either results dir
**/model.pt

# Logs and Python cruft
*.log
**/log.txt
__pycache__/
*.pyc
*.pyo
.DS_Store

# Local data index variants (regenerable)
data/index_10k.json*

# transformers-packaged build output (weights on HF)
hf_model/
hf_export/
_hf_upload.py

# Generated index files (regenerable via build_*_index.py; index_transfer is 611 MB)
data/index_*.json
data/index_*.json.bak

# transformers build output for the scaled model (weights on HF)
hf_export_v2/

# scratch scripts and chain drivers
_*.py
_*.sh
